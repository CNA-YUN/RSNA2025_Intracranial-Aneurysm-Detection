你这个 `requirements.txt` 是典型的 **CUDA 12.4 + PyTorch 2.6 GPU 环境**，里面有很多 GPU 专用包，比如：

- `torch==2.6.0+cu124`
- `torchvision==0.21.0+cu124`
- `torchaudio==2.6.0+cu124`
- 所有 `nvidia-*`
- `triton==3.2.0`
- `cucim-cu12`
- `cupy-cuda12x`
- `torch_cluster==1.6.3+pt26cu124`

这些在纯 CPU 电脑上要么装不上，要么装了也没用。**不要直接 `pip install -r requirements.txt`**。

下面给你一套可操作的 CPU 环境配置方案。

---

## 1. 创建虚拟环境

推荐 Python 3.10 或 3.11。

```bash
conda create -n med_cpu python=3.11 -y
conda activate med_cpu
```

如果你不用 conda，用 venv 也可以：

```bash
python -m venv med_cpu
# Windows
med_cpu\Scripts\activate
# Linux/macOS
source med_cpu/bin/activate
```

---

## 2. 先安装 CPU 版 PyTorch

这是最关键的一步。必须从 PyTorch 官方 CPU 源安装：

```bash
pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu
```

安装后验证：

```bash
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

正常应输出类似：

```text
2.6.0+cpu
False
```

如果输出 `2.6.0+cu124`，说明装错了，需要先卸载再重装。

---

## 3. 生成 CPU 版 requirements

复制一份 `requirements.txt`，命名为 `requirements_cpu.txt`，然后删除所有 CUDA/GPU 专用行。

Linux/macOS 可以直接用命令生成：

```bash
grep -v -E '^(torch==|torchvision==|torchaudio==|triton==|nvidia-|cucim-cu12==|cupy-cuda12x==|torch_cluster==)' requirements.txt > requirements_cpu.txt
```

Windows PowerShell：

```powershell
Get-Content requirements.txt | Where-Object { $_ -notmatch '^(torch==|torchvision==|torchaudio==|triton==|nvidia-|cucim-cu12==|cupy-cuda12x==|torch_cluster==)' } | Set-Content requirements_cpu.txt
```

也就是删除这些：

```text
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
triton==3.2.0
nvidia-*
cucim-cu12==25.8.0
cupy-cuda12x==13.6.0
torch_cluster==1.6.3+pt26cu124
```

注意：`torch-geometric==2.6.1` 要保留，它是纯 Python 包，CPU 可用。

---

## 4. 安装其余依赖

```bash
pip install -r requirements_cpu.txt
```

如果下载慢，可以先换国内 PyPI 源：

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

但 PyTorch CPU 版仍然建议用官方源安装，即第 2 步。

---

## 5. 如果项目需要 torch_cluster

`torch_cluster` 是 PyG 的扩展，CPU 版可以尝试从 PyG 官方源装：

```bash
pip install torch_cluster -f https://data.pyg.org/whl/torch-2.6.0+cpu.html
```

如果失败，也可以先不装，运行代码看是否真的用到。很多项目只是 import 了但实际没用。

如果还缺 `torch_scatter`、`torch_sparse`，同样方式：

```bash
pip install torch_scatter torch_sparse -f https://data.pyg.org/whl/torch-2.6.0+cpu.html
```

---

## 6. 修改代码里的 GPU 硬编码

CPU 机器上，`torch.cuda.is_available()` 会返回 `False`，所以最好统一写成：

```python
import torch

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model.to(device)
data = data.to(device)
```

需要搜索项目代码并修改：

- `.cuda()` → `.to(device)`
- `device='cuda'` → `device=device`
- `torch.load(...)` → `torch.load(..., map_location='cpu')`
- `torch.cuda.amp.autocast()` → 禁用或改成 CPU 兼容写法
- `DataParallel` → 直接去掉，CPU 不需要多卡并行
- `pin_memory=True` → 可改成 `False`

例如：

```python
# 原来
model = MyModel().cuda()
x = x.cuda()

# 改成
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = MyModel().to(device)
x = x.to(device)
```

加载模型时：

```python
checkpoint = torch.load("model.pth", map_location="cpu")
```

混合精度 AMP 在 CPU 上建议先关掉：

```python
# with torch.cuda.amp.autocast():
#     output = model(input)
```

或者：

```python
scaler = torch.cuda.amp.GradScaler(enabled=False)
```

---

## 7. 常见报错与处理

### 报错：`No module named 'cupy'`
说明代码用了 CuPy。CPU 环境没有 CuPy，通常需要改成 NumPy。可以全局搜索 `cupy`，把相关逻辑替换成 `numpy`。

### 报错：`No module named 'cucim'`
cuCIM 是 GPU 图像处理库，CPU 上一般没有替代。如果只是读取全切片图像，可考虑 `openslide-python`、`tifffile`、`imagecodecs`。

### 报错：`libcudart.so not found`
说明还在用 CUDA 版 PyTorch。重新安装 CPU 版：

```bash
pip uninstall torch torchvision torchaudio -y
pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu
```

### 报错：`torch_cluster` 安装失败
先注释掉相关 import，确认项目是否真的需要。若必须，尝试从源码编译，但 Windows 上较麻烦。

### 报错：NumPy 兼容问题
如果出现 `np.float_` 之类错误，可能是某些包不支持 NumPy 2.x。可以降级：

```bash
pip install "numpy<2"
```

---

## 8. 如果项目是 nnU-Net

这个依赖列表很像 nnU-Net 相关项目。如果是 nnU-Net，CPU 下可以这样：

```bash
export nnUNet_compile=0
```

训练：

```bash
nnUNetv2_train DATASET_ID CONFIG FOLD -device cpu
```

推理：

```bash
nnUNetv2_predict -i INPUT -o OUTPUT -d DATASET_ID -c CONFIG -f FOLD -device cpu
```

CPU 训练会非常慢，只适合调试。

---

## 9. 最小命令总结

```bash
conda create -n med_cpu python=3.11 -y
conda activate med_cpu

pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu

grep -v -E '^(torch==|torchvision==|torchaudio==|triton==|nvidia-|cucim-cu12==|cupy-cuda12x==|torch_cluster==)' requirements.txt > requirements_cpu.txt

pip install -r requirements_cpu.txt

# 可选
pip install torch_cluster -f https://data.pyg.org/whl/torch-2.6.0+cpu.html
```

然后修改代码中的 `.cuda()`，再运行。

---

## 最后建议

纯 CPU 适合：
- 跑通代码
- 小样本调试
- 推理少量数据
- 数据预处理

不适合：
- 大规模训练
- 3D 医学图像分割训练
- 大模型微调

如果你要真正训练，建议用云 GPU，比如 AutoDL、阿里云、腾讯云、恒源云，或者 Colab。把 `requirements.txt` 原样拿去装 GPU 环境会更省事。